# sudo без пароля
sudo visudo
www-data ALL=(root) NOPASSWD: /bin/systemctl reload sing-box

sudo visudo
django ALL=(root) NOPASSWD: /bin/kill -HUP *

#Запуск с пересборкой 
docker-compose up -d --build

# удалить всех пользователей
curl -X DELETE http://vpnzi.ru:8001/api/v1/users/delete_all/

# конфигурация
nano /etc/sing-box/config.json

